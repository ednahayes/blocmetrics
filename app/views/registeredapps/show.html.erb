<div class="container">
  <h1><%= @registeredapp.name %></h1>
</div>
<div class="container">
	<h4>URL: <%= link_to @registeredapp.url, "#{@registeredapp.url}" %></h4>
</div>	
<div class="container">
    <small>
     submitted <%= time_ago_in_words(@registeredapp.created_at) %> ago
     by <%= @registeredapp.user.email %>
    </small>
</div> 
<div class="container">
 <h3>EVENTS:</h3> 
  <div class="row">
    <div class="col-md-4">
     <% @registeredapp.events.each do |event| %>
      <div class="media">
        <div class="media-body">
          <h4 class="media-heading">
            <p><%= event.name %></p>
          </h4>
        </div>
      </div>
     <% end %>
    </div>
  </div>    
</div>
<div class="container">
   <%= pie_chart @registeredapp.events.group(:name).count %> 
</div> 
<div class="container">
   <%= line_chart @registeredapp.events.group_by_day(:created_at).count %>
</div>
<div class="container">
    <%= link_to "Edit Registered Application", edit_registeredapp_path, class: 'btn btn-success' %><br/>
    <%= link_to "Delete Registered Application", @registeredapp, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this application?' } %>
</div>
